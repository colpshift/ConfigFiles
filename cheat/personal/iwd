# interactive prompt
iwctl

# list all available commands
[iwd]# help

# list all Wi-Fi devices
[iwd]# device list

# scan for networks
[iwd]# station device scan

# list all available networks
[iwd]# station device get-networks

# connect to a network
[iwd]# station device connect SSID

# Show device and connection information
[iwd]# device device show

# display the connection state
[iwd]# station device show

# list networks you have connected to previously
[iwd]# known-networks list

# forget a known network
[iwd]# known-networks SSID forget

# disconnect from a network:
[iwd]# station device disconnect

# passphrase supply as a command line argument
iwctl --passphrase passphrase station device connect SSID

